# PostgreSQL connection string (required for API)
DATABASE_URL=postgresql://user:password@localhost:5432/livetrack

# Local API server port (default: 5000)
PORT=5000

# Vite dev server (optional overrides)
# PORT=5173
# BASE_PATH=/
# API_PROXY_TARGET=http://127.0.0.1:5000
